{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "v0.1 default capabilities (graph read-only)",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
