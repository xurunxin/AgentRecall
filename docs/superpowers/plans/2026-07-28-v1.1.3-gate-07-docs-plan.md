# v1.1.3 GATE-07 — Implementation Plan

Date: 2026-07-28
Lane: C1 (Phase C — Documentation and final publication)
Sub-issue: [#37](https://github.com/xurunxin/AgentRecall/issues/37)
Design spec: `docs/superpowers/specs/2026-07-28-v1.1.3-gate-07-docs-design.md`
Branch: `feat/v1.1.3-gate-07-docs`
Worktree: `.worktrees/v113-gate-07-docs`
PR target: `main`
Target commit base: `23a8934` (Phase A + B merged)

---

## Strategy decisions (locked)

- **Single PR**, closes #37. ~ 1500 lines of README + docs + tests.
- **TDD ordering**: RED → GREEN for the doc tests; the README
  rewrite follows once the tests pin the contract.
- **No new dependencies**.
- **No schema migration**.
- **Backward compat**: the v1.1.2→v1.1.3 migration is schema-preserving
  (the v1.1.2 schema is preserved; the `import_batches` lineage table
  + `audit_metadata_json` column are added additively).

## Commit ordering

| # | Commit | Scope | Verification |
|---|--------|-------|--------------|
| 1 | `test(v113-gate-07): documentation tests RED scaffold` | NEW `test/release-gate/v113-documentation.test.ts` + `test/release-gate/v113-distribution-contract.test.ts` | both files FAIL against current README |
| 2 | `docs(v113-gate-07): rewrite README + cross-link Phase A + B ADRs and guides` | `README.md` rewrite | doc tests pass |
| 3 | `docs(guide): verify Phase A + B guide set and ADR cross-links` | verify guides from Phase A + B exist + are cross-linked | `node -e "..."` smoke |
| 4 | `docs(example): verify examples/ matches v1.1.3 contract` | verify the `examples/` directory | `npx tsc --noEmit` on examples |
| 5 | `chore(v113-gate-07): final verification + CHANGELOG update` | `npm test` + typecheck + build + verify:artifacts | All exit 0 |

## Per-commit detail

### Commit 1 — Documentation tests

```text
test/release-gate/v113-documentation.test.ts (NEW)
test/release-gate/v113-distribution-contract.test.ts (NEW)
```

`v113-documentation.test.ts` covers:
- README contains the canonical entry paths (`dist/src/index.js` for MCP, `dist/bin/agent-recall.js` for CLI).
- README references the v1.1.3 ADRs (`0004`, `0005`, `0006`, `0007`, `0008`).
- README uses the canonical platform vocabulary (`linux-x64` / `darwin-x64` / `win32-x64`).
- README does not contain `windows-x64` (the v1.1.2-era token).
- README has no claims that contradict the v1.1.3 behavior
  (e.g., no "lookup mode may create a new identity" — that was the v1.1.2 bug #31 closed).
- README badge URL is `xurunxin/AgentRecall`.
- README references the operator guides from Phase A + B.
- README "Installation" section describes the canonical-platform artefact + sha256 verify + extract + npm install + run.
- README "Upgrade / Rollback" section exists.

`v113-distribution-contract.test.ts` covers:
- `package.json` `files` array contains `dist`, `README.md`, `LICENSE`, `CHANGELOG.md`.
- `LICENSE` file exists at the project root.
- `dist/src/index.js` exists after `npm run build`.
- `dist/bin/agent-recall.js` exists after `npm run build`.
- The CLI `agent-recall --version` outputs `1.1.3`.
- The server-version (`src/server-version.ts`) exports `1.1.3`.

### Commit 2 — README rewrite

```text
README.md (MODIFIED)
```

Rewrite the README to align with v1.1.3:

- **Header**: title + description + badge URL `xurunxin/AgentRecall`.
- **Requirements**: Node 24, npm, MCP-compatible client.
- **Architecture**: `MemoryService` façade + the three sub-services.
- **Setup**: build + run; canonical entry paths.
- **MCP vs CLI**: `agent-recall-mcp` (MCP stdio) + `agent-recall` (CLI).
- **Data directory**: `~/.agent-recall/` + `AGENT_RECALL_HOME`.
- **Tools**: Core / Extended / Admin profile tool lists.
- **Project Identity**: 3 modes + canonical registration path + reference `docs/adr/0004-identity-resolution-modes.md` + `docs/guides/identity-resolution.md`.
- **Capabilities**: profile-scoped + load-time permission validation + reference `docs/adr/0005-profile-scoped-admin-capability.md` + `docs/guides/operator-capability.md`.
- **Sensitivity**: 3×3 visibility matrix + reference `docs/adr/0006-one-sensitivity-policy.md` + `docs/guides/sensitivity-matrix.md`.
- **Installation**: canonical-platform artefact + sha256 verify + extract + `npm install --omit=dev` + run.
- **Upgrade / Rollback**: v1.1.2 → v1.1.3 (schema-preserving) + rollback recipe.
- **Development**: how to run tests + the per-suite scripts + the orchestrator.

### Commit 3 — Verify guide set and ADR cross-links

```text
docs/guides/ (verify)
docs/adr/ (verify)
```

Verify each guide + ADR exists and is cross-linked from the README. The Phase A + B guides already exist:
- `docs/adr/0004-identity-resolution-modes.md`
- `docs/adr/0005-profile-scoped-admin-capability.md`
- `docs/adr/0006-one-sensitivity-policy.md`
- `docs/adr/0007-release-evidence-contract.md`
- `docs/adr/0008-deterministic-orchestration.md`
- `docs/guides/identity-resolution.md`
- `docs/guides/operator-capability.md`
- `docs/guides/sensitivity-matrix.md`
- `docs/guides/release-publication.md`
- `docs/guides/release-test-topology.md`

### Commit 4 — Verify examples/ matches v1.1.3

```text
examples/ (verify)
```

Walk the `examples/` directory. Verify:
- All examples import from the v1.1.3 entry points (`dist/src/index.js` or `dist/bin/agent-recall.js`).
- No v1.1.2-era examples remain.
- TypeScript examples compile cleanly via `npx tsc --noEmit --project examples/`.

### Commit 5 — Final verification + CHANGELOG

```text
CHANGELOG.md (MODIFIED — append GATE-07 bullet under the existing `## [1.1.3]` header)
```

```bash
cd .worktrees/v113-gate-07-docs
npm run build                # exit 0
npm run typecheck            # exit 0
npm test                     # <N> passed / 0 skipped (Core profile)
npm run verify:artifacts     # exit 0
npx vitest run test/release-gate/v113-documentation.test.ts test/release-gate/v113-distribution-contract.test.ts   # exit 0
```

Append evidence to #37 closing comment via:
```
gh issue comment 37 --body '<snippet>' --repo xurunxin/AgentRecall
```

## Files touched (final list)

| Path | Status | Lines (est.) |
|------|--------|--------------|
| `README.md` | MODIFIED | + 400 / − 350 |
| `examples/` | MODIFIED | variable |
| `CHANGELOG.md` | MODIFIED | + 50 |
| `test/release-gate/v113-documentation.test.ts` | NEW | + 250 |
| `test/release-gate/v113-distribution-contract.test.ts` | NEW | + 150 |

Total: ~ 850 lines added, ~ 350 removed.

## Risk register (lane-local)

| Risk | Mitigation |
|------|-----------|
| External links may break | The README rewrite is sourced from the existing structure; only the broken refs are updated. |
| Bad example runs in CI | The documentation tests execute every example claim in CI; a bad example fails the test. |
| Broken back-compat section | The v1.1.2 → v1.1.3 migration is schema-preserving; the rollback recipe is the same as v1.1.2's (restore backup). |

## Definition of done (lane-level)

- [ ] All 5 commits pushed to `feat/v1.1.3-gate-07-docs`.
- [ ] `npm test` (Core / Admin / Extended) → green, 0 skipped.
- [ ] `npm run typecheck` → exit 0.
- [ ] `npm run build` → exit 0.
- [ ] `npm run verify:artifacts` → exit 0.
- [ ] README contains the v1.1.3 contract.
- [ ] Distribution contract tests pass.
- [ ] CHANGELOG v1.1.3 section has the GATE-07 bullet.
- [ ] PR description references `Parent Gate: #30` and `Closes #37`.
- [ ] Evidence snippet captured for the #37 closing comment.
- [ ] No new dependencies.

Once these close, the lane is ready for release-cutter review. C2 (#38) follows on the same CHANGELOG header.
