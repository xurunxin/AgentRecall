# AgentRecall 项目协作规则

本文件供所有在本项目工作的 AI 编码 agent / 协作者阅读。规则高于个人偏好,冲突时以本文件为准。

## 工作准则(八荣八耻)

> 这些是协作底线,不是建议。任何对其中一条的违反,都应当在 PR / 提交说明里被指出并修复。

- 以**臆猜接口**为耻,以**查档求证**为荣
  接触陌生 API / 类型 / 行为前,先读源码、注释、测试、CHANGELOG。允许不确定,但不允许装作确定。

- 以**模糊开工**为耻,以**对齐需求**为荣
  在动键盘前确认:目标是什么?影响范围?验收标准?模糊的"先做着看"不进入主分支。

- 以**脑补业务**为耻,以**请示规则**为荣
  业务规则来自需求方 / 现有代码 / 文档,不是来自推断。拿不准就问,问不出来就标注 TODO,不要自己造。

- 以**新增冗余**为耻,以**复用存量**为荣
  新增文件、函数、依赖前先查:已有吗?能扩吗?能复用吗?AgentRecall 尤其重视"零新依赖、零行为回归"。

- 以**省略校验**为耻,以**完备测例**为荣
  改一处就补一类测:单元、集成、smoke、回归。覆盖正常路径 + 边界 + 失败路径。

- 以**乱改架构**为耻,以**恪守规范**为荣
  MemoryService 拆好的三层(read / write / maintenance)有明确边界,helpers 有明确归属。改前先读 `src/services/` 下的现状,改后保持边界不变。

- 以**不懂装懂**为耻,以**坦诚存疑**为荣
  不确定就明说"我不确定这块",并给出下一步验证计划。绝不输出没把握的结论。

- 以**批量乱改**为耻,以**分步迭代**为荣
  一次 PR / 一次提交只解决一件事。拆不开就分多个提交,不要图省事一把梭。

## 适用范围

- 所有在 `src/`、`test/`、`docs/`、`bin/`、`dist/`(构建产物除外)下的改动
- 所有 `package.json`、`tsconfig.json`、`vitest.config.ts` 等构建/依赖配置
- 所有 `CHANGELOG.md` 更新

## 违反怎么办

发现违反上述任一条的改动,无论来自人或 agent:
1. 在 review / 反馈里直接指出对应条款;
2. 给出符合规则的改法;
3. 必要时阻塞合并,直到修复。

新增条款请走 PR + 评审,不要单方面追加。
